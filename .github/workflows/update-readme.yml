name: Update README

on:
  schedule:
    - cron: '*/1 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity

    steps:
      - uses: actions/checkout@v2
      - uses: sa24449/github-activity-readme@main
        env:
          GITHUB_TOKEN: ${{ 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 }}
